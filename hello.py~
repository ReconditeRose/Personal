import re
for test_string in ['555-1212', 'ill-egal']:
	if re.match(r'^\b[0-9]{2}-\d{4}$', test_string):
		print test_string, 'is valid IS local number'
	else:
		print test_string, 'is not a valid number'
